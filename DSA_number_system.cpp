#include<iostream>
using namespace std;

// number systems : 

// Decimal number systems : Uses 10 digits (0-9) and is also known as base-10. 
// Binary number systems : Uses 2 digits (0)and (1) and is also known as base-2.
// hexadecimal number systems : hexadecimal uses base 16 (digits 0-9 and letters A-F). 



int main()
{
    // code 1 : conversion of binary to decimal
    /*int num;
    cout<<"Enter a binary number : ";
    cin>>num;

    int ans=0, power=1, lastdigit;
    while(num>0)
    {
        lastdigit = num % 10;
        ans += lastdigit * power;
        power *= 2;
        num /= 10;
    }
    cout<<"Decimal no is : "<<ans;*/


    //code 2 : conversion of decimal to binary
    /*int n;
    cout<<"Enter a decimal number : ";
    cin>>n;

    int ans=0, power=1;
    while(n>0)
    {
        int paritydigit = n % 2;
        ans += paritydigit * power;
        power *= 10;
        n /= 2;
    }
    cout<<ans;*/

    
}




